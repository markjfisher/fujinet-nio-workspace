---
topic: Amiga FUMOUNT handler teardown and clean resident-driver unload
updated: 2026-09-03T10:55
---

- (note) Sources: backlog/amiga-fumount-clean-driver-removal.md (LLM Q&A), current Amiga FUMOUNT in nio-core-apps, disk/nio Expunge in fujinet-nio-driver, Amiberry diskdevice-fmount sequences.
- (direction) Build against fujinet-nio-driver Expunge plus FUMOUNT mechanisms; FUMOUNT work ships first; then disk-device unload; Amiberry sequence+pytest must prove both.
- (decision) One epic in this spec folder (existing BMad pattern: SPEC.md + companions + stories.yaml), not a classic BMM PRD/epics.md path.
- (capability) CAP-1: Operator can FUMOUNT DNx: so the DOS handler no longer holds that unit open and FujiNet media is ejected.
- (capability) CAP-2: FUMOUNT on a busy volume reports busy and must not eject media.
- (capability) CAP-3: After all DNx handlers are retired, fujinet-disk.device OpenCnt can reach zero so RemDevice is allowed to complete.
- (capability) CAP-4: RemDevice(fujinet-disk.device) can unload the disk-device binary (real Expunge: tear down, Remove, return LoadSeg seglist).
- (capability) CAP-5: After disk device is gone, RemDevice(fujinet-nio.device) can unload the broker (existing Expunge already returns seglist).
- (capability) CAP-6: An unload CLI reports whether the named device left DeviceList, via a fresh FindName after RemDevice, never the old pointer.
- (capability) CAP-7: After unload, fujinet-load-resident can load new disk and nio binaries without rebooting.
- (capability) CAP-8: New Amiberry sequence+pytest prove FUMOUNT handler teardown (busy refuse + success path).
- (capability) CAP-9: New Amiberry sequence+pytest prove disk-then-nio unload, reload, and a post-reload FMOUNT/Dir.
- (capability) CAP-10: Existing FMOUNT/FUMOUNT/FMOUNTRESTORE Amiberry cases remain passing.
- (constraint) Delivery order is a gate: FUMOUNT handler-teardown before changing disk-device Expunge to actually unload.
- (constraint) Unload order: stop/dismount users of disk device, RemDevice disk, RemDevice nio, then load nio, then load disk.
- (constraint) Never forcibly remove a live block device from under an active filesystem handler.
- (constraint) Implement DOS teardown with classic packets (DeviceProc, ACTION_FLUSH, ACTION_DIE), not OS 3.2 Dismount and not Assign DISMOUNT.
- (constraint) Expunge refuses/defers while OpenCnt!=0, queue nonempty, or a request is in progress; do not abort live I/O to force unload.
- (constraint) Disk Expunge that actually unloads must stop the worker, fn_transport_close, discard change registrations, Remove the node, free the base, return segment_list.
- (constraint) After RemDevice, status is a new FindName; do not dereference the pre-RemDevice pointer.
- (constraint) Verification: cheapest owner gate plus new Amiberry nodes with startup/*.sequence and tests.toml; default guest wb32 + a1200-030.
- (constraint) Do not reopen Stage 8/Phase 2 or HDF/RDB; FUMOUNT remains catalogue DNx: workflow.
- (note) Brownfield: Amiga FUMOUNT today Inhibit+TD_EJECT+uninhibit; FMOUNT already has ACTION_DIE in retire_handler; disk complete_pending_expunge closes transport but returns 0 and does not Remove/free/return seglist; nio device_expunge already unloads.
- (assumption) Assumed FUMOUNT stays the nio-core-apps C:FUMOUNT command (architecture + README), even though Expunge lives in fujinet-nio-driver; fujinet-mount remains diagnostic-only.
- (assumption) Assumed after successful ACTION_DIE the DNx: DOS node may remain (dol_Task NULL) so later FMOUNT/Dir can respawn the handler — unless REMOVE is accepted.
- (assumption) Assumed Kickstart 1.3 packet compatibility in code, certification on existing Amiberry WB 3.x guest (same as current FMOUNT suite).
- (question) Does everyday FUMOUNT DNx: replace Inhibit+eject+uninhibit with ACTION_DIE+eject, or is DIE a new flag/subcommand while current eject-keep-handler stays default?
- (question) Is FUMOUNT DNx: REMOVE (RemDosEntry so DN cannot respawn) in this epic?
- (question) Confirm owning repos: nio-core-apps for FUMOUNT vs moving logic into fujinet-nio-driver.
- (question) Unload CLI: new fujinet-unload-resident in the driver tools tree, one device name per invocation?
- (direction) Story Breakdown: fumount first, then disk Expunge, then unload CLI, then Amiberry unload/reload sequence.
- (decision) Story 1 FUMOUNT handler teardown; Story 2 disk Expunge unload; Story 3 unload CLI; Story 4 Amiberry unload/reload. spec_checkpoint and done_checkpoint true on story 1; done_checkpoint true on story 4.
- (note) Wrapper-only dropped: chat tone, verbatim FlushDevice C listing (pattern kept), OS 3.2 Dismount trial as an experiment not a product path.
- (event) Self-validate pass 1 coherence: Spec Law 1-6 and 8 hold; CAP-4 success names teardown steps also required as constraints; HOW for packets lives in constraints/companions.
- (event) Self-validate pass 2 preservation: backlog load-bearing claims landed in SPEC or brownfield/delivery/lifecycle; Assign DISMOUNT and OS 3.2 Dismount excluded as implementation; REMOVE left as open question.
- (event) stories.yaml schema check: four quoted ids 1-4, required fields, no status, prefix-free, execution order fumount first.
- (decision) Everyday FUMOUNT DNx: is DIE-then-eject; that is the complete meaning of unmount. Do not keep eject-but-leave-handler-alive as default; no distinct use case for that.
- (constraint) FUMOUNT must not Inhibit-keep-handler as the unmount path; Flush then ACTION_DIE then eject is the only FUMOUNT success path.
- (decision) FUMOUNT DNx: REMOVE / RemDosEntry is out of this epic. A later purge-DOS-registration debug tool would be a separate command, not FUMOUNT.
- (non-goal) No FUMOUNT REMOVE flag; no RemDosEntry in FUMOUNT; no product default that ejects media while leaving the handler running.
- (assumption) After successful ACTION_DIE the DNx: DOS node may remain (null task); later FMOUNT or volume access may restart the handler. That is accepted because REMOVE is out of scope.
- (direction) CAP-10: update existing fmount/fumount Amiberry assertions that encoded keep-handler-after-eject so they match DIE-then-eject; cases still pass.
- (event) Self-validate pass 1 coherence: DIE-then-eject is the FUMOUNT constraint; REMOVE and keep-handler-eject are non-goals; CAP IDs unchanged.
- (event) Self-validate pass 2 preservation: user answers 1-2 absorbed; backlog REMOVE/two-level fumount distinction now out of epic except as rejected.
- (note) stories.yaml story 1 still matches CAP-1/2/8; invoke_dev_with still hedges keep-handler-after-eject — offer Story Breakdown re-run to drop that hedge.
- (decision) FMOUNT and FUMOUNT stay as Amiga-specific sources in nio-core-apps; edit apps/platform/amiga/fumount.c.
- (decision) Apps consume the driver only through the published DiskDevice SDK (build/amiga/include + libfujinet-amiga-disk.a). Workspace core-apps-amiga runs make sdk first. Do not include driver-private headers or share driver .c.
- (note) fumount.c already uses SDK only for FUJINET_DISK_DEVICE_NAME and OpenDevice/TD_EJECT. ACTION_DIE is dos.library. DIE-then-eject needs no new driver ABI.
- (note) fmount.c already links SDK support (classify/envec). That is why -lfujinet-amiga-disk exists; fumount does not need extra SDK symbols for DIE.
- (decision) Unload CLI is fujinet-unload-resident beside fujinet-load-resident in the driver tools tree; one Exec device name per invocation.
- (assumption) Native disk Expunge tests prove busy deferral and returned seglist under FUJINET_DISK_NATIVE_TEST (may skip real Remove/FreeMem). Guest Amiberry Story 4 proves the device name is gone from DeviceList after RemDevice.
- (question) Q3 restated: native tests cannot cheaply simulate Exec DeviceList; guest sequence can. Assumed split unless user objects.
- (event) Self-validate pass 1: owners/SDK/unload CLI pinned; Open Questions omitted; CAP IDs stable.
- (event) Self-validate pass 2: SDK coupling and fumount DOS-only DIE path preserved in brownfield; Q3 answered as native vs guest split assumption.
- (constraint) ACTION_FLUSH failure is fail-safe: FUMOUNT does not continue to ACTION_DIE or eject. This epic does not treat unsupported FLUSH as success.
- (constraint) The DNx: filesystem handler must reject ACTION_DIE while unsafe/busy (locks, handles, cwd, notifications). Do not attribute that refusal to DOS/DoPkt itself.
- (constraint) Post-unmount proof of handler absence must not DeviceProc or access DNx: (Dir/Type/DeviceProc can restart the handler). Observe DOS-list dol_Task (e.g. doslistdiag) and media status via disk.device, not volume access.
- (constraint) FUMOUNT's temporary OpenDevice for eject must CloseDevice; with no other clients, fujinet-disk.device lib_OpenCnt is 0. Observe OpenCnt without opening DNx:.
- (constraint) ACTION_DIE is used because it is the classic/OS 1.3 packet for handler terminate; later OS deprecation and OS 3.2 Dismount are not the implementation.
- (direction) Success signal: terminate every DNx handler, not idle every DNx handler.
- (decision) Re-derive stories.yaml dispatch notes so Story 1 tests match FLUSH fail-safe, handler-owned DIE reject, no DeviceProc absence proof, and OpenCnt==0 after CloseDevice.
- (event) Self-validate pass 1: six review items in kernel; success signal terminate; FLUSH fail-safe; handler-owned DIE; no DeviceProc absence proof; OpenCnt after CloseDevice; ACTION_DIE for OS 1.3.
- (event) Self-validate pass 2: review items also in delivery/lifecycle/brownfield; stories.yaml invoke_dev_with re-derived to match.
- (event) stories.yaml schema check: ids 1-4 quoted, prefix-free, no status; story 1 checkpoints unchanged.
