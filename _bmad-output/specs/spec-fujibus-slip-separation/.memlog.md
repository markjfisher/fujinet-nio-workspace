---
topic: Extract SLIP framing from FujiBusTransport into a distinct IFramer layer
updated: 2026-08-29T13:49
---

- (decision) Source: _bmad-output/planning-artifacts/backlog-fujibus-slip-separation.md — absorbed as primary input
- (decision) Slug: fujibus-slip-separation (derived from backlog filename)
- (capability) CAP-1: FujiBusTransport delegates all framing to an injected IFramer; it contains no SLIP byte constants, no _rxBuffer, and no reference to 0xC0 or SlipByte::End — success: grep over fujibus_transport.cpp/.h finds zero SLIP references; POSIX fujibus-pty-debug ctest suite passes green
- (capability) CAP-2: SlipFramer implements IFramer carrying all existing SLIP logic (extractSlipFrame, _rxBuffer, C0-delimiter handling including consecutive-C0/stale-delimiter bug fix) — success: all existing SLIP framing tests pass when targeted at SlipFramer::nextPacket directly, including C0-consecutive-delimiter cases
- (capability) CAP-3: NativeFramer implements IFramer as a pass-through or length-prefix framer for packet-native channels with no SLIP logic — success: NativeFramer compiles and is reachable from a build-profile selection point; its unit tests cover pass-through/length-prefix correctness
- (capability) CAP-4: Existing POSIX/RS-232/PTY/USB-CDC builds construct SlipFramer+FujiBusTransport; a future Zorro build constructs NativeFramer+FujiBusTransport — success: build-profile factory wires correct framer by construction with no #ifdef to select framer type
- (constraint) No #ifdef to select framer — selection is by construction at the factory/profile level; violating this re-couples the concerns the refactor separates
- (constraint) Scope limited to repos/fujinet-nio; Amiga driver (fujinet-nio-driver), Amiga lib (fujinet-nio-lib), and Amiga serial backend (fujinet_nio_serial_backend.c / fn_session.c) are not touched
- (constraint) All existing ctest tests must remain green after the refactor — no regression permitted
- (constraint) FujiBusTransport integration tests (test_embed_core.cpp InMemoryChannel pattern) remain unchanged — they drive the full stack and must not be rewritten to accommodate the split
- (decision) Non-goals: any new physical channel (Zorro hardware, SPI); Amiga lib framing changes; BMAD spec work for Zorro or floppy; write support; runtime framer selection (selection is build-time)
- (note) Files to change: src/lib/fujibus_transport.cpp, include/fujinet/io/transport/fujibus_transport.h, tests/test_fujibus_transport_framing.cpp, channel factory/build-profile wiring
- (note) New files: include/fujinet/io/transport/iframer.h, include/fujinet/io/transport/slip_framer.h, src/lib/slip_framer.cpp, include/fujinet/io/transport/native_framer.h (or similar), src/lib/native_framer.cpp
- (note) AC from backlog: (1) FujiBusTransport has zero SLIP refs; (2) SlipFramer passes all SLIP tests incl C0-consecutive cases; (3) fujibus-pty-debug ctest green; (4) NativeFramer compiles and reachable from build profile; (5) no #ifdef for framer selection
- (decision) Companion brownfield.md spawned: holds IFramer interface contract, construction patterns, file change table, test strategy, and scope boundary — all too detailed for kernel
- (event) Self-validate Pass 1 (Coherence): PASSED — CAP-1..4 each have intent+success; intents are WHAT not HOW (interface contract in brownfield.md); all constraints are decision-bending; non-goals explicit; success signal is grep+ctest demonstrable; IDs unique
- (event) Self-validate Pass 2 (Preservation): PASSED — IFramer contract → brownfield.md; construction patterns → brownfield.md; file change table → brownfield.md; test strategy → brownfield.md; AC 1-5 from backlog → CAP-1..4 success criteria + success signal; scope boundary → Constraints + Non-goals; wrapper narrative dropped (logged)
- (direction) User: trust spec, no spec_checkpoint, no done_checkpoint on any story
- (direction) User: invoke_dev_with must ensure existing SLIP framing tests move across to SlipFramer, C0-consecutive-delimiter cases preserved, and new tests added per brownfield.md test strategy
- (decision) Story 1: IFramer interface — iframer.h pure virtual contract only
- (decision) Story 2: SlipFramer — all SLIP logic migrated from FujiBusTransport; existing framing tests retargeted to SlipFramer::nextPacket; C0-consecutive-delimiter cases must survive
- (decision) Story 3: FujiBusTransport refactor — inject IFramer&, remove _rxBuffer and all SLIP refs, POSIX builds wire SlipFramer by construction, full ctest green
- (decision) Story 4: NativeFramer + build-profile wiring — pass-through/length-prefix stub, notional Zorro profile wired by construction no #ifdef, NativeFramer unit tests added
- (event) stories.yaml written: 4 stories (IFramer interface, SlipFramer, FujiBusTransport refactor, NativeFramer+wiring); validity check: ids unique, quoted strings, no status field, no id prefix collisions — PASSED
