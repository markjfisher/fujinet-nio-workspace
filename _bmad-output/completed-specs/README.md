# Completed BMad specs

Archived spec folders, moved here from `../specs/` after every story was
`done` and the matching workspace goal was accepted.

Keep the folder shape (`SPEC.md`, companions, `stories.yaml`, `stories/`)
so the contract stays readable. Do not edit these as living requirements;
durable product behaviour lives in `docs/` (and repo READMEs). To reopen
work, move the folder back to `../specs/` and treat it as an update.
