# Archived BMad artifacts

Documents here are **historical snapshots**. They must not drive
implementation, experiments, or agent context.

If a later research report, spec, or `docs/` contract superseded a file,
use that later source. Read an archive file only to recover what was
believed at the time and which of those beliefs were later rejected.
