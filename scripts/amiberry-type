#!/usr/bin/env bash
# Compatibility shim — delegates to: scripts/amiga type [options] <text>
set -euo pipefail
SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
exec "$SCRIPT_DIR/amiga" type "$@"
